# Process Notes

## /onboard
- **Technical experience:** Senior engineer, 20 years across web, games, VR/AR. Currently Python, C#/Unity, React/TS, Anthropic SDK, LangChain. Has built MCP servers and chatbots.
- **Learning goals:** Agent orchestration — wants to move from building individual AI components to coordinating multi-agent systems.
- **Creative sensibility:** Appreciates slick, polished tools (Raycast/Glaze). Puzzle game fan (point-and-click, Wordle). House music DJ for 15+ years — values flow, transitions, craft.
- **Prior SDD experience:** Strong informal practice — pen-and-paper game design, Miro for architecture, Claude Code for planning. Already does this instuitively, just hasn't formalized it.
- **Background context:** Career spans multiple domains, currently fully immersed in the AI wave. Not just following — actively building and experimenting.
- **Energy and engagement:** Confident, direct, knows what he wants to learn. Moves fast. Calibrate for a senior engineer who wants depth, not hand-holding.

## /scope
- **Idea evolution:** Started as a vague "financial research chatbot," then narrowed to portfolio analysis + risk, then sharpened to "enter a ticker, get a buy/not recommendation." Final pivot: instead of the app deciding buy/sell, it presents the case for all three (Buy/Hold/Sell) and lets the user decide. This was the learner's own insight — a significant upgrade to the concept.
- **Pushback received:** Challenged on scope — portfolio tracking, multi-stock, risk analysis all cut. Kamen accepted the cuts pragmatically ("it's fair to make it simpler given the timeframe") without resistance. Also pushed on data sources — acknowledged real data matters but agreed to scope to what's wirable in 3-4 hours.
- **References that resonated:** Incite AI resonated most — right depth, wrong presentation. Kamen immediately critiqued the cluttered UI. VectorVest's clear signal structure influenced the three-card layout. WarrenAI was dismissed as "just a chatbot with general information."
- **Design direction:** Kamen drove this strongly — rejected dark mode as "overused in financial apps," chose light/calm/serif/editorial. Drew on his Raycast/Glaze aesthetic. The "analyst note, not Bloomberg terminal" framing captured his vision.
- **Deepening rounds:** 0 explicit deepening rounds chosen — the learner opted to lock in after mandatory questions. The conversation was rich enough through the mandatory flow that the scope doc had strong material.
- **Active shaping:** High. Kamen drove multiple key decisions: the three-perspectives model (instead of a single recommendation), the editorial design direction, the light mode choice, the expandable cards UX. The implicit news connections angle was his from the start. He pushed back on generic chatbot framing early and consistently moved toward something with real opinion and craft.

## /prd
- **Scope changes from scope doc:** Kamen added a two-stage validation flow (inline validation on input screen → transition to results screen only on success → separate error handling for agent failures). This wasn't in the scope doc and shows strong UX thinking — distinguishing "bad input" from "backend failure" with different UI responses.
- **What surprised them:** Nothing dramatically surprised Kamen — he's experienced enough to anticipate most edge cases. The caching complexity was a self-catch: he proposed recent tickers caching, then proactively flagged it as potentially too complex for the timeframe and agreed to defer it.
- **Scope guard:** Caching (last 5 tickers with same-day invalidation) was moved to "What we'd add with more time." Kamen initiated this himself — recognized the persistence/timestamp logic would eat into build time. Clean decision, no resistance.
- **Design convictions:** Smooth transitions between all states was a strong requirement — "I don't want raw cuts." Consistent with his DJ sensibility and Raycast/Glaze aesthetic. Also insisted on user-friendly agent progress messages ("Reading Apple's latest earnings...") rather than technical logs.
- **Deferred decisions:** Source link format (inline vs. reference list) deliberately left open — Kamen wants to see real data before deciding. Pragmatic call.
- **Deepening rounds:** 0 rounds. Kamen opted to proceed after mandatory questions. The conversation was efficient — he gave clear, decisive answers and the core flow was well-defined without needing additional probing.
- **Active shaping:** High. Kamen designed the two-stage error handling flow on the spot, self-regulated scope by flagging caching complexity, and made clear aesthetic calls (smooth transitions, friendly progress text). Asked for help researching fundamentals — knows when to delegate domain knowledge he doesn't have. Overall: decisive, scope-aware, design-driven.

## /spec
- **Stack chosen:** Next.js 15 + Tailwind (pnpm), FastAPI + Python 3.11, LangGraph 1.0+ for orchestration, Anthropic SDK direct (Sonnet 4.6 for fundamentals + news, Opus 4.6 for synthesis), yfinance for market data. Kamen drove the LangGraph choice — explicitly wants to learn it.
- **Deployment:** Local-only. Kamen flagged token-burn concerns with public deployment unprompted — strong cost-awareness instinct.
- **Architecture decisions made by Kamen:**
  - LangGraph over hand-rolled or Claude Agent SDK (learning goal-driven)
  - Single synthesis call producing all three arguments (chose coherence over per-argument depth)
  - Two endpoints (`/validate` + `/research`) over single endpoint
  - Fundamentals agent upgraded from deterministic-only to LLM-summary call ("more interesting")
  - Dual-source news (yfinance + web_search) with 3-5 implicit connection cap
  - Opus for synthesis, Sonnet for news — explicit cost/quality split
  - Fail hard on synthesis failure (no Sonnet fallback) — chose UX honesty over demo robustness
  - Confidence indicator per card ("strong/moderate/thin")
  - Strategy C for progress messages (each node writes status_message as first action) — explicit "less business logic on UI" preference
  - 1.2s artificial minimum display time for progress messages, enforced backend-side
  - Monorepo, early header SSE emit, prompts as `.md` files for editability
- **Confident on:** Stack choices, two-endpoint shape, fail-hard policy, monorepo, prompt-files-as-markdown, cost awareness throughout.
- **Uncertain on:** Will likely iterate on which fundamental metrics to display ("I will probably add different metrics and data at later stage, but I want to start somewhere"). Pragmatic — happy to ship v1 and refine.
- **Deepening rounds:** 0 chosen. Kamen opted for option A (proceed to spec) after Phase 1 mandatory questions — matches the pattern from /scope and /prd. Rich Phase 1 conversation gave the spec strong material without needing additional probing.
- **Active shaping:** Very high. Kamen made every meaningful technical call himself — picked LangGraph, picked Opus/Sonnet split, upgraded fundamentals to LLM call, called the fail-hard policy, picked the confidence indicator, picked Strategy C explicitly with reasoning ("less business logic on UI"), endorsed artificial pacing on first hearing it, chose monorepo + early header emit + .md prompts in one decisive answer. He's in the driver's seat — the role here is competent technical sparring partner, not architect.
- **PRD open questions resolved:** Q1 (header metrics curation). Q2 + Q3 deferred to /build as planned.

## /checklist
- **Sequencing decision:** Backend-first, inside-out. Kamen noted his usual instinct is frontend-first with mocks, but explicitly flipped for this project because the learning target (LangGraph + SSE) lives in the backend — strong self-aware sequencing call. Graph skeleton with stubs (step 2) and SSE pacing (step 3) land before any LLM/yfinance work so the new/risky surface is battle-tested early. Endpoints and real agents layer on top (4–7). Frontend consumes a known-good SSE contract (8–9b). Polish + submit closes (10).
- **Tests:** Kamen asked to add pytest coverage on his own initiative — another signal he's taking the backend learning seriously, not just rushing to a demo.
- **Build mode:** Step-by-step. Aligned with learning goal — wants to see the LangGraph wiring unfold rather than review a monolithic autonomous build.
- **Comprehension checks:** Yes for backend/graph items (2–7), off for frontend items. Tailored choice — he recognized frontend is familiar territory and wouldn't benefit from checks, but LangGraph/SSE mechanics should be actively recalled.
- **Verification:** Yes — per-item. Accepted without pushback.
- **Check-in cadence:** Learning-driven. Wants decision narration during build, especially on the new orchestration surface.
- **Git cadence:** Commit per step with `step N: [title]` message format. Accepted immediately.
- **Demo tickers:** AAPL (safe, rich) + FIG/Figma (learner's suggestion — thematic fit with his design sensibility, fresh IPO surfaces implicit-connection material well). Good creative call.
- **Gitignore:** Kamen created the local repo mid-conversation and asked for a proper `.gitignore`. Written to cover Python (venv, pytest/mypy/ruff caches), Next.js (node_modules, `.next`, pnpm), env files with `.env.example` preserved, macOS/IDE noise, and retains his pre-existing `.claude/` rule.
- **Item count + time:** 11 items (one split — 9 → 9a/9b at Kamen's agreement), ~3.5 hours estimated. Backend 1–7 ≈ 2hr, frontend 8–9b ≈ 1hr, polish/submit ≈ 30min.
- **Split rationale (9 → 9a/9b):** Original step 9 bundled SSE consumer + progress + header + cards + error + 5 components in one item — too big for one step-by-step session. Kamen agreed immediately when flagged. 9a focuses on streaming mechanics + header, 9b focuses on the payoff UI (cards + error).
- **Submission planning:** Core story lead is three-perspectives + LangGraph orchestration as learning angle. Wow moment identified as implicit connections. Local-only deployment confirmed; demo video recommended (60-90s, YouTube unlisted). GitHub repo already created locally with remote set — no repo-creation needed in submission item.
- **Confident on:** Sequencing (backend-first flip was his call), methodology choices, git cadence, demo tickers.
- **Needed guidance on:** Granularity of step 9 (accepted split when flagged), Devpost submission planning details.
- **Deepening rounds:** 0 chosen. Pattern from /scope, /prd, /spec holds — Kamen moves decisively through mandatory questions and opts to proceed. Rich mandatory-phase conversation gave the checklist strong material without additional probing.
- **Active shaping:** High. Kamen drove the backend-first flip (against his own default), added pytest coverage on his own initiative, calibrated comprehension checks by domain (backend yes, frontend no), suggested FIG as a second demo ticker tied to his design identity, and agreed to the 9-split quickly when risk was flagged. Decisive, self-aware, owns the plan.

## /build

### Step 1: Project scaffolding (backend + frontend skeletons)
- **What was built:** Backend and frontend scaffolds were already in place from prior work — verified backend `pyproject.toml` has all required deps (fastapi, uvicorn, langgraph, anthropic, yfinance, pydantic, python-dotenv, sse-starlette, pytest, pytest-asyncio), `main.py` stubs `GET /health`, `.env.example` present. Frontend is Next.js 16 + React 19 + Tailwind 4 + App Router via pnpm, with `.env.local.example` set.
- **Issues encountered:** `uv sync` failed with `yfinance>=1.2.2` — pin was wrong (real yfinance versioning is 0.2.x). Kamen fixed the pin himself after I flagged it; rejected my edit attempt.
- **Learner verification:** Confirmed "both the frontend and backend works" after the pin fix.
- **Notable:** `frontend/AGENTS.md` warns this Next.js version has breaking changes — docs live in `node_modules/next/dist/docs/`. Must consult those before writing frontend code in steps 8–9b.

### Step 2: LangGraph skeleton with stub nodes + state + tests
- **What was built:** `backend/graph/state.py` (ResearchState TypedDict with `status_message` using a last-value-wins reducer to handle parallel writes from fundamentals + news), four stub nodes under `backend/graph/nodes/` each returning a `status_message` + placeholder payload, `backend/graph/graph.py` wiring `START → validate → fan-out[fundamentals, news] → synthesis → END`. Added `backend/tests/test_graph.py` with 3 cases (node sequence, fan-out before synthesis, status messages populated). Configured pytest-asyncio auto mode + pythonpath in pyproject.
- **Spec-vs-reality drift:** Spec snippet shows `add_conditional_edges` with `{"gather": ["fundamentals", "news"]}` as the path-map. LangGraph's current API rejects a list as a mapping destination (`TypeError: unhashable type: 'list'`). Fix: router function returns the list directly; no path-map needed. Worth revisiting spec.md if we want it to match the real code.
- **Decisions narrated:** (1) `status_message` reducer — parallel fan-out writes the same key, so `Annotated[..., last_value_reducer]` is required. (2) Stub-node shape — single-dict return (no mid-node stream_writer) is sufficient for step 2; real Strategy C emission (message before heavy work) wires in steps 4–7 once there's actual heavy work to pace.
- **Verification:** Kamen ran the graph-sequence print and confirmed "looks ok" — validate → fundamentals + news (both between) → synthesis.
- **Comprehension check:** "Signals parallel fan-out" — correct on first try.
- **Issues encountered:** One spec-API drift caught during `pytest` (list-as-path-map error); fixed in <1 min.

### Step 3: SSE pacing wrapper + `/research` endpoint (stubbed graph)
- **What was built:** `backend/sse.py` with `format_sse(event, data)` (wire format `event:...\ndata:...\n\n`), `pace_events(source, min_gap=1.2)` async generator enforcing the minimum inter-event gap, and `graph_events(compiled, state)` adapter that maps LangGraph's `astream(stream_mode="updates")` output into `(event, data)` tuples (progress / result / error). Wired `POST /research` in `main.py` using `sse-starlette`'s `EventSourceResponse`, yielding `{"event", "data"}` dicts. Added `backend/tests/test_sse.py` covering wire format, default + custom min-gap pacing, and immediate-first-emit.
- **Import-path cleanup (mid-step):** Kamen flagged that `from backend.x import ...` broke at runtime because he runs uvicorn from inside `backend/`. Rewrote all imports to top-level (`from graph.state import ...`, `from sse import ...`), changed pytest `pythonpath` from `[".."]` to `["."]`, added a comment in `pyproject.toml` documenting the convention, and saved a feedback memory so this convention is preserved across sessions. Tests + runtime resolution now match.
- **Decisions narrated:** (1) Pacing in the SSE wrapper, not nodes — keeps graph logic pure, presentation contract lives at the edge. (2) `pace_events` yields tuples (not pre-formatted strings) so the endpoint can hand sse-starlette dicts and avoid double-wrapping; `format_sse` stays as the test-side single source of truth for wire format. (3) Adapter pattern (`graph_events`) decouples LangGraph's update shape from SSE event vocabulary — keeps mapping logic in one place for steps 4–7 to extend (header event, etc.).
- **Verification:** Kamen ran `curl -N -X POST localhost:8000/research ...` and reported "seems to run fine."
- **Comprehension check:** "Keeps nodes pure; UI/timing concern at edge" — correct on first try.
- **Issues encountered:** sse-starlette double-formatting almost-bug (caught while writing main.py — initial draft yielded pre-formatted SSE strings, which sse-starlette would have wrapped a second time). Restructured before committing. Plus the import-path issue Kamen flagged, fixed with the convention lock-in above.


### Step 4: Real `/validate` endpoint + validate node (yfinance)
- **What was built:** `backend/clients/yfinance_client.py` with `TickerNotFoundError` (carries the friendly PRD-mandated error message) and `fetch_info(ticker)` that normalizes case/whitespace and raises on empty/missing-`longName`. Replaced stub `validate.py` to call the shared client. Added `POST /validate` to `main.py` returning `{valid:true, company_name}` or `{valid:false, error}`. Dedicated `backend/tests/test_validate.py` with 10 cases mocking `yfinance.Ticker` directly (client layer + node + endpoint). Added `backend/tests/conftest.py` with an autouse fixture that mocks `fetch_info` for graph/SSE tests (keeps them off the network) and a `no_mock_yfinance` marker so validate tests opt out.
- **Decisions narrated:** Shared client so `/validate` endpoint + graph node use identical error semantics (single source of friendly message). Autouse mock + opt-out marker isolates network-touching tests.
- **Issue encountered (yfinance 1.2.0 bug):** First live curl returned 500. Built `backend/scripts/debug_yfinance.py` to probe. Root cause: `yfinance 1.2.0` `_fetch_complementary()` raises `YFException` on `TRAILING_PEG_RATIO` fetch from Yahoo ("Failed to generate key") — crashes `.info` / `.get_info()` before returning data. Initial fix: retry-once workaround (second call returned cached partial data). Then Kamen asked about newer versions — PyPI had **1.2.2** (released 2026-04-13).
- **Dependency upgrade:** Bumped `yfinance>=1.2.2`. Two blockers hit: (a) no Windows Py3.14 wheel for 1.2.2 — fixed by `requires-python <3.14` and `[tool.uv] environments = ["sys_platform == 'darwin'"]` (local-only project, fine). (b) Kamen's global `~/.config/uv/uv.toml` has `exclude-newer = "1 week"` which blocked 1.2.2 (2 days old) — overrode in project pyproject with `[tool.uv] exclude-newer = "2026-04-20"`. 1.2.2 confirmed to return full dict (182 keys) on first call — dropped the retry workaround.
- **Learner verification:** "seems to work fine now" after 1.2.2 upgrade (live AAPL + ZZZZZ curls).
- **Comprehension check:** "Defense-in-depth at the graph entry" — correct on first try. Kamen grasped the `/research`-direct-hit threat model.
- **Active shaping:** Kamen drove the yfinance upgrade path — asked "what's the newest version" rather than accepting the retry hack. Good engineering instinct to fix the root cause over the workaround.

### Step 5: Fundamentals agent (real yfinance + Sonnet) + early header SSE emit
- **What was built:** `backend/clients/anthropic_client.py` (lazy-cached `AsyncAnthropic` + model constants `MODEL_SONNET`/`MODEL_OPUS`), `backend/prompts/__init__.py` with `load_prompt(name)` helper + `fundamentals_summary.md` prompt (editorial, 2–3 paragraphs, no recommendations). Real fundamentals node: yfinance fetch, `_build_header` with 6 metrics + computed `change_pct`, `_build_raw_metrics` with 8 extra fields, `_summarize_with_sonnet` call. SSE layer now consumes `stream_mode=["updates","custom"]`; header is emitted via `get_stream_writer()` inside the node BEFORE the Sonnet call. Dedicated `test_fundamentals.py` (8 cases, including a slow-summary test that proves the header arrives while Sonnet is still blocked). Conftest autouse mock extended to patch `fetch_info` + `_summarize_with_sonnet` on the fundamentals node.
- **Decisions narrated:** (1) Direct Anthropic SDK over `langchain-anthropic` — full control for upcoming news agent `web_search`. (2) Originally built the early-emit using plain updates-mode detection (SSE wrapper spotting `fundamentals.header` in the returned delta). Kamen caught during verification that this puts header + summary in the same update — header was arriving AFTER Sonnet finished, not before. Switched to mid-node `get_stream_writer()` + `stream_mode=["updates","custom"]`, which matches spec.md's "as it appears in state" wording and actually delivers the early UX the spec promises. (3) Test strengthened with `asyncio.Event` synchronization to *prove* ordering (header observed while slow summary still blocked), not just "header appears somewhere in the stream." (4) Scope-creep caught: initial draft of graph_events included upstream-error short-circuits for `fundamentals_error`/`news_error` — pulled back out; that's step 7 territory where synthesis properly routes state.error.
- **Learner verification:** First live curl — Kamen flagged header and summary emitted "almost at the same time, header is not emitted beforehand." Real bug, not perception. After switching to `get_stream_writer()` + multi-mode stream: confirmed "now it works." Also manually inspected the Sonnet summary via a one-off script — read like analyst prose.
- **Comprehension check:** `stream_mode=["updates","custom"]` — correct on first try. Kamen connected the mechanism immediately once he'd seen the fix land.
- **Issues encountered:** The early-emit bug above — important learning moment about LangGraph's update granularity (one node = one `updates` delta no matter how many state writes happen inside, unless you use the custom channel).
- **Active shaping:** Very high. Kamen caught a real ordering bug during verification that the initial test didn't cover — the kind of thing tests-first can miss because the test was written from the same (wrong) mental model as the code. He re-verified manually after the fix, not just trusting green tests.

### Step 6: News agent (yfinance + Anthropic `web_search` dual-source)
- **What was built:** `fetch_news()` added to `clients/yfinance_client.py` — normalizes yfinance 1.2.x's nested `{content: {...}}` shape (and legacy flat shape) into `{title, publisher, published_at, url}`, swallows exceptions returning `[]`. Real `news.py` node: yfinance headline pull → Sonnet 4.6 call with `[web_search_20250305, emit_news]` tools → structured payload with `direct_news` / `macro_context` / `implicit_connections` (capped at 5). `asyncio.wait_for` timeout wrapper. Fallback: on timeout OR any exception, build a minimal payload from yfinance headlines (only errors out if yfinance is *also* empty). `prompts/news_analyst.md` pushes hard on cross-domain implicit connections. New `test_news.py` (8 cases: happy path, cap, timeout-fallback, exception-fallback, exception+no-yfinance-error, tool-extraction helpers, multi-iteration loop). Conftest extended to stub `news_node.fetch_news` + `_analyze_with_sonnet` for the graph/SSE tests. Debug helper script `scripts/run_news.py` with `--timeout` flag + structured logging.
- **Decisions narrated:** (1) **Custom tool + server tool in one call.** Used `emit_news` (client-defined structured schema) alongside `web_search_20250305` (Anthropic server tool). Server tools are fulfilled inline inside a single `messages.create` — Claude can search several times and then call `emit_news` all in one response. No hand-rolled agentic loop for the common case; kept a small MAX_TOOL_ITERATIONS safety loop with nudge-message in case Claude stops without emitting. (2) **Fallback-on-anything.** Spec says fall back to yfinance-only if web_search fails; broadened to any exception so flaky Anthropic calls also degrade gracefully rather than killing the run. Only sets `news_error` when both paths have zero data — synthesis can then route to the error terminal. (3) **Cap implicit_connections at 5 in code, not just prompt.** Defensive — prompts drift. (4) **Sector pulled from `state.fundamentals.header` if present.** In the fan-out, fundamentals may or may not have populated yet when news starts; `.get()` chain handles both.
- **Spec-vs-reality drift (noted):** Spec said "30s soft timeout" for the Anthropic call. Reality: real web_search + Sonnet reasoning on AAPL took ~60-90s on a cold call. Bumped `NEWS_TIMEOUT_SECONDS` default to 90.0 so the happy path doesn't hit the fallback. The 30s number was a PRD-era guess before we'd seen web_search latency firsthand.
- **Learner verification:** First live run (`scripts/run_news.py AAPL`) hit the 30s timeout — Kamen flagged it. Added per-iteration logging (stop_reason, block types including `server_tool_use(web_search)`, usage, elapsed) and a `--timeout` flag on the debug script. Re-ran with `--timeout 120` → full structured payload returned with all three sections populated. Kamen confirmed "it's returning all the needed data."
- **Comprehension check:** Correct on first try — "News gathers evidence; synthesis reasons over it." Clean separation-of-concerns read. Sets up step 7 well (synthesis gets a fixed evidence frame to reason over).
- **Issues encountered:** (1) Default 30s timeout was too tight (fixed above). (2) `scripts/run_news.py` failed with "graph could not be found" because `uv run python scripts/...` puts `scripts/` on sys.path, not `backend/` — added an explicit `sys.path.insert` at the top of the script. (3) `test_sse_emits_header_event_before_summary_completes` started timing out because it runs the full compiled graph under the `no_mock_yfinance` marker, and the news node now does real I/O — extended that test file's autouse fixture to stub the news node's externals too.
- **Active shaping:** Kamen asked for debug logging when the timeout hit (good instinct — "see what's actually happening" over "bump the number and hope"), which is what exposed that 30s was just wrong, not that the agent was broken.

### Step 7: Synthesis agent (Opus 4.6 + `emit_briefing` tool) + final `result` SSE event
- **What was built:** `backend/schemas/briefing.py` with `Citation`/`Argument`/`Briefing` pydantic models (`confidence` typed as `Literal["strong","moderate","thin"]` — belt-and-suspenders with the tool input_schema enum). `backend/prompts/synthesis.md` system prompt pushing for genuine disagreement, grounded-in-evidence claims, calibrated confidence, prose-not-bullets. Real synthesis node: `EMIT_BRIEFING_TOOL` schema requiring `buy/hold/sell` each with `{summary, reasoning, confidence, citations}`; forced `tool_choice={"type":"tool","name":"emit_briefing"}` on Opus 4.6; `_extract_emit_briefing()` helper; `Briefing.model_validate()` + `model_dump()` back into `state.briefing` so the existing SSE `delta.briefing → result` branch fires. Fail-hard policy: upstream `fundamentals_error`/`news_error` propagate the original message (so UI shows *why*); self-errors (Opus exception or pydantic ValidationError) set a friendly ticker-scoped state.error. No Sonnet fallback. `test_synthesis.py` (9 cases: happy path, both upstream short-circuits, Opus exception, extractor present/missing, full `_synthesize_with_opus` parse + raise-when-tool-missing, ValidationError via bad confidence enum). `conftest.py` + `test_fundamentals.py` extended to stub `_synthesize_with_opus` for full-graph tests. New `test_graph_events_emits_result_for_happy_path` in test_sse.py runs the compiled graph end-to-end and asserts `result` event with `{buy,hold,sell}`.
- **Decisions narrated:** (1) Forced `tool_choice` over text/JSON-mode — guarantees structured output, same pattern as `emit_news`. (2) `Literal` enum on `confidence` in *both* the input_schema and the pydantic model — prompts drift; the confidence tag is UX-load-bearing so belt-and-suspenders. (3) Fail-hard with asymmetric messaging: upstream errors show the real reason (debuggable), self-errors show friendly prose (logs have detail). (4) No new SSE plumbing — the step-3 adapter already maps `delta.briefing → result`; step 7 just fills state.briefing. Adapter pattern paid off.
- **Learner verification:** Manual curl for AAPL. Kamen shared the full `result` payload. Read together: three cases split on a coherent axis (Buy = near-term catalyst + India offsets, Hold = middle-of-range with clustered cost headwinds, Sell = dual-squeeze thesis where hardware cost pressure + Services legal pressure hit Apple's two margin pillars simultaneously). Implicit connection (Services-as-margin-buffer-under-legal-attack) actually loads the Sell reasoning — non-obvious linkage, not headline restatement. Citations back the claims. Prose committed rather than hedged. PRD "three distinct perspectives" criterion met.
- **Calibration observation:** All three AAPL cases came back `moderate`. Defensible but slightly undersells the confidence field — the Sell case with 5 converging threads (tariffs, memory, GM, Services legal, China share) arguably earned `strong`. Opus choosing the safer tag. Worth watching on FIG; if every case across tickers lands at `moderate`, the prompt may need a nudge that strong/thin are allowed to surface. Not a step-7 blocker.
- **Comprehension check:** Why one Opus call rather than three parallel? "Coherence — the cases can reference each other" — correct. Kamen connected that the AAPL output he just read is proof: Sell's dual-squeeze thesis only works because it contrasts with Buy's near-term frame; parallel calls would have all landed near the moderate middle.
- **Issues encountered:** None — step 7 came together with no runtime issues. The step-3 SSE adapter design plus the step-6 `emit_news` pattern meant step 7 was mostly assembly + prompt craft + schema discipline.
- **Open thread for later:** FIG verification (fresh IPO, thin dossier) deferred — would be the real test of whether `thin` confidence surfaces. If Kamen runs it before frontend work, note the result; otherwise pick it up during polish in step 10.
- **Active shaping:** Kamen ran the live AAPL verification and read the three cases substantively (not just "three blobs of text came back"), flagged the moderate-everywhere calibration question implicitly by sharing the full payload for critique. Senior-engineer verification pattern — evaluate the quality of the output, not just that it exists.

